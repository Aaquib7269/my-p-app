<p id="notice"><%= notice %></p>

<p>
  <strong>Category:</strong>
  <%= @brand.category %>
</p>

<p>
  <strong>Name:</strong>
  <%= @brand.name %>
</p>

<% for item_model in @brand.item_models %>
	<%= item_model.name %> <br />
<% end %>

<%= link_to 'Edit', edit_brand_path(@brand) %> |
<%= link_to 'Back', brands_path %>
